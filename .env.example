# MTProxy Configuration
# Copy this file to .env and customize as needed

# Proxy port (default: 443)
# Use 443 to bypass most firewalls
PORT=443

# Statistics port (default: 8888)
# Access stats at http://your-ip:8888/stats
# For security, consider disabling this in production
STATS_PORT=8888

# Number of worker threads (default: 2)
# Increase for higher traffic (recommended: number of CPU cores)
WORKERS=2

# Secret key for connections (leave empty to auto-generate)
# Format: 32-character hexadecimal string (16 bytes)
# Generate with: head -c 16 /dev/urandom | xxd -ps
# Example: ee00000000000000000000000000000000
SECRET=

# Telegram advertising tag (optional)
# Get yours from @MTProxybot on Telegram
# This allows you to promote channels through your proxy
AD_TAG=
